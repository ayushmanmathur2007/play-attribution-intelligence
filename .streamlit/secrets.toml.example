# Copy to .streamlit/secrets.toml for local dev, or paste the contents into
# the "Secrets" tab in Streamlit Community Cloud for a deployed app.
# .streamlit/secrets.toml is gitignored — never commit real keys.

ANTHROPIC_API_KEY = "sk-ant-..."
